import { LemonWebApp } from '@/components/lemon-web-app';

/**
 * Main mobile web entry page.
 *
 * Returns:
 *   The Lemon AID web feature-test surface.
 */
export default function HomePage() {
  return <LemonWebApp />;
}
