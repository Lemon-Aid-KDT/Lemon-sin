{
  "mcpServers": {
    "supabase": {
      "type": "http",
      "url": "https://mcp.supabase.com/mcp?project_ref=${SUPABASE_PROJECT_REF}&read_only=${SUPABASE_MCP_READ_ONLY}&features=${SUPABASE_MCP_FEATURES}"
    }
  }
}
