# Previous Version Deliverables

이 폴더는 P1 안정화 이전에 작성된 기존 P1 문서·발표 산출물을 보존하기 위한 보관 위치다.

현재 구현 기준으로 실행되는 백엔드 코드, 데이터 fixture, CI 설정은 프로젝트의 활성 경로에 유지한다. 이 폴더의 문서는 의사결정 이력과 이전 설계 근거를 확인할 때만 사용한다.

`root-copy-*` 파일은 프로젝트 루트에 별도 생성되어 있던 동일 산출물을 이 폴더로 모은 보관본이다.
