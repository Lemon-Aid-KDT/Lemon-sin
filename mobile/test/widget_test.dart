// Placeholder smoke test.
// 원본 `flutter create` 카운터 보일러플레이트(`MyApp`)는 제거됨 — 실 앱은 `LemonAidApp`.
// 의미 있는 widget test 는 화면별로 추후 추가 예정.

import 'package:flutter_test/flutter_test.dart';

void main() {
  test('placeholder', () {
    expect(1 + 1, 2);
  });
}
