// widgets/supplement_preview.dart — D1 셸
//
// 담당: B UI/UX
// 참조: 분석 결과 미리보기 (§3.7)

import 'package:flutter/material.dart';

import '../utils/tokens.dart';

class SupplementPreview extends StatelessWidget {
  const SupplementPreview({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder(
      fallbackHeight: 80,
      color: LemonColors.brand,
    );
  }
}
