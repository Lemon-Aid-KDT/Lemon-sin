// widgets/ai_input_sheet.dart — D1 셸
//
// 담당: B UI/UX
// 참조: 챗봇 입력 시트 (§7.3.3 챗봇 Agent)

import 'package:flutter/material.dart';

import '../utils/tokens.dart';

class AiInputSheet extends StatelessWidget {
  const AiInputSheet({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder(
      fallbackHeight: 80,
      color: LemonColors.brand,
    );
  }
}
