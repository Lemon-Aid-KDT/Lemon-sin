# Database
POSTGRES_USER=lemon
POSTGRES_PASSWORD=lemon1234
POSTGRES_DB=lemon_aid
POSTGRES_HOST=localhost
POSTGRES_PORT=5432

# JWT
JWT_SECRET=your-secret-key-change-this-in-production
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30
REFRESH_TOKEN_EXPIRE_DAYS=7

# Redis
REDIS_URL=redis://localhost:6379/0

#google api
GOOGLE_CLIENT_ID=

# kakao api
# (모바일 SDK access_token 만으로 검증하므로 백엔드에 키 불필요)

# ─── Email (Resend) ───
# https://resend.com → API Keys 에서 발급. 형식: re_xxxxxxxxxxxx
# 운영 단계에서는 도메인 인증 후 EMAIL_FROM 을 자체 도메인으로 교체.
RESEND_API_KEY=
EMAIL_FROM=Lemon Aid <onboarding@resend.dev>