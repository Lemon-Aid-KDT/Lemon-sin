fastapi==0.115.0
uvicorn[standard]==0.30.6
sqlalchemy[asyncio]==2.0.35
asyncpg==0.30.0
alembic==1.13.3
pydantic[email]==2.9.2
pydantic-settings==2.5.2
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
# bcrypt 4.x removed `__about__`, breaking passlib's bcrypt backend detection
# → fallback path probes 72-byte limit and raises ValueError on every hash.
# Keep bcrypt < 4 until we migrate off passlib.
bcrypt<4
python-multipart==0.0.12
httpx==0.27.2
redis==5.0.8
