# Core Web
# Cap below 0.137: FastAPI 0.137 reworked include_router into a deferred
# `_IncludedRouter`, so routes no longer surface as flat APIRoute objects on
# app.routes and the RLS route-seam guard (which introspects create_app()) breaks.
# The app runs on 0.138 too, but pin to the developed/tested line for determinism.
fastapi>=0.110,<0.137
uvicorn[standard]>=0.27,<0.35

# Pydantic v2
pydantic>=2.6,<3.0
pydantic-settings>=2.2,<3.0

# Auth
PyJWT[crypto]>=2.10,<3.0

# DB
sqlalchemy[asyncio]>=2.0,<3.0
asyncpg>=0.29
alembic>=1.13

# Cache
redis>=5.0

# External APIs
google-cloud-vision>=3.7
google-auth>=2.29
httpx>=0.27
ollama>=0.6.0

# Image
pillow>=10.2

# Utils
python-multipart>=0.0.9
# OCR accuracy metrics (CER/WER/field match) — used by src/ocr/metrics.py
rapidfuzz>=3.0
