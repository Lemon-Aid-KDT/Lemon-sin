# Runtime Nutrition Reference Subset

This directory contains only the small KDRIs and nutrient reference files needed
by the Docker runtime. Large crawling images, simulator captures, and source raw
archives remain under the repository-level `data/nutrition_reference` tree and
must not be copied into the backend image.
