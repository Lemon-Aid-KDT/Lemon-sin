# Capability Evals

Capability evals prove that the Agent harness can perform a behavior that did
not previously exist.

Initial capability gates:

- supplement preview stops before persistence
- meal evaluation returns a stable schema
- chat reminder request returns `ToolPreview`
- revoked consent blocks Agent execution

