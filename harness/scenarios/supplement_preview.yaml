{
  "id": "supplement_preview",
  "description": "Supplement analysis stops at preview and never stores raw OCR text.",
  "user_fixture": "users/consented_user.json",
  "analysis_fixture": "analysis_results/supplement_preview.json",
  "memory_fixture": "agent_memory/basic_memory.json",
  "steps": [
    {"type": "agent", "agent": "personalization"},
    {"type": "agent", "agent": "evaluation"},
    {"type": "tool_preview", "tool": "extract_supplement_facts"}
  ],
  "expect": {
    "status": "passed",
    "required_agents": ["personalization", "evaluation"],
    "required_tool_previews": ["extract_supplement_facts"],
    "forbidden_log_fields_absent": true,
    "side_effects": []
  }
}

