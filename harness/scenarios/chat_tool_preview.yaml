{
  "id": "chat_tool_preview",
  "description": "Chat reminder request returns a ToolPreview instead of creating a reminder.",
  "user_fixture": "users/consented_user.json",
  "analysis_fixture": "analysis_results/chat_reminder_request.json",
  "memory_fixture": "agent_memory/basic_memory.json",
  "steps": [
    {"type": "agent", "agent": "chat"},
    {"type": "tool_preview", "tool": "add_reminder"}
  ],
  "expect": {
    "status": "passed",
    "required_agents": ["chat"],
    "required_tool_previews": ["add_reminder"],
    "forbidden_log_fields_absent": true,
    "side_effects": []
  }
}

