{
  "id": "consent_revoked",
  "description": "Revoked consent blocks Agent execution before any Agent or Tool step.",
  "user_fixture": "users/revoked_user.json",
  "analysis_fixture": "analysis_results/meal_evaluation.json",
  "memory_fixture": "agent_memory/basic_memory.json",
  "steps": [
    {"type": "agent", "agent": "personalization"},
    {"type": "agent", "agent": "evaluation"}
  ],
  "expect": {
    "status": "blocked",
    "block_reason": "consent_required",
    "required_agents": [],
    "required_tool_previews": [],
    "forbidden_log_fields_absent": true,
    "side_effects": []
  }
}

